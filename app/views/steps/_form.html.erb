<%= form_for([@task, @task.steps.build]) do |f| %>
  <p>
    <%= f.label :stepwriter %><br />
    <%= f.text_field :stepwriter %>
  </p>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
    <%= f.submit %>
  </p>
<% end %>